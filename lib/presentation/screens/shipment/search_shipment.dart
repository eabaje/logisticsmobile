import 'package:flutter/material.dart';

class SearchShipment extends StatelessWidget {
  const SearchShipment({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}