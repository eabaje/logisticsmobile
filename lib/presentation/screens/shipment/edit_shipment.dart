import 'package:flutter/material.dart';

class EditShipment extends StatelessWidget {
  const EditShipment({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}