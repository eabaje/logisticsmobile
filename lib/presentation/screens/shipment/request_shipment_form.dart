import 'package:flutter/material.dart';

class RequestShipmentForm extends StatefulWidget {
  const RequestShipmentForm({ Key? key }) : super(key: key);

  @override
  _RequestShipmentFormState createState() => _RequestShipmentFormState();
}

class _RequestShipmentFormState extends State<RequestShipmentForm> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}